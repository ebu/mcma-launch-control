task copyTerraformBin(type: Copy) {
    from "externals/terraform"
    into "build/staging/bin"
}

build {
    dependsOn copyTerraformBin
    fileMode 0755
}

tsc {
    dependsOn ":libraries:commons:npmInstall"
    dependsOn ":libraries:data:npmInstall"
}
