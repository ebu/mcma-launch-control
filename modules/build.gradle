task build {
    dependsOn "job-processor-service:build"
    dependsOn "job-repository:build"
    dependsOn "media-repository:build"
    dependsOn "aws-s3-bucket:build"
    dependsOn "service-registry:build"
}
