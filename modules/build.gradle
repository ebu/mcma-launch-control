task build {
    dependsOn "aws-job-processor-service:build"
    dependsOn "aws-job-repository:build"
    dependsOn "aws-media-repository:build"
    dependsOn "aws-s3-bucket:build"
    dependsOn "aws-service-registry:build"
}
